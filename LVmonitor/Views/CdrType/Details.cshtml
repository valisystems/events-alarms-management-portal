@model LvMonitor.Models.Lvm_CdrType

<div class="lvm-box">
    <div class="lvm-box-header">
        <span>@ViewBag.Title @ViewBag.SubTitle</span>
    </div>
    <div class="lvm-box-body">
        <div>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.CdrType)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.CdrType)
                </dd>
            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.CdrTypeId })
        </p>
    </div>
</div>
